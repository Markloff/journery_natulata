// import { Project, ProjectType } from './project';
//
//
// const toProjectId = (type: ProjectType, name: string) => `${type}_${name}`;
//
// export class ProjectCollection {
//
// 	private readonly store: Map<string, Project> = new Map<string, Project>();
//
// 	constructor(private readonly _hasFn: (element: Project) => string) {
// 	}
//
// 	lookupOrInsert(type: ProjectType, name: string): Project {
// 		const projectId =
// 		if (this.store.has(toProjectId(type, name))) {
// 			return this.store.get()
// 		}
// 		return this.store.has(toProjectId(type, name));
// 	}
//
// }
//
//
//
//
